# 应用系统等级保护要求逐项核查报告（应用层核查）

## 1. 核查说明

- 核查日期：2026-08-24
- 核查对象：Admin X（Vue 3 + NestJS + Node.js `node:sqlite`）当前代码、配置模板和测试结果
- 对照依据：[应用系统等级保护要求设计规划要点.md](./应用系统等级保护要求设计规划要点.md)，该文档引用 GB/T 22239-2019 的身份鉴别、访问控制、安全审计、入侵防范、恶意代码防范、数据完整性、数据保密性、备份和恢复、剩余信息保护、个人信息保护要求
- 核查方式：逐项阅读源文档，对照前端、API、数据库和测试；只确认代码中可复核的应用层控制

本报告只用于确认应用层代码能力，不对网络、主机、外部安全设备和组织管理事项作结论；WAF/IDS/IPS/EDR、TLS/VPN、漏洞扫描平台、异地备份、高可用和审批制度等不计入应用层未完成项。

### 状态定义

| 状态             | 含义                                             |
| ---------------- | ------------------------------------------------ |
| 已完成（应用层） | 代码中有可复核实现，且本地测试或静态检查可以验证 |
| 未完成（应用层） | 当前代码或应用控制流程存在明确缺口               |
| 不适用           | 当前不存在对应业务场景，不计入未完成项           |

## 2. 先给结论

当前应用层已经实现账号唯一约束、RBAC、最小权限、登录失败锁定、服务端会话台账与指定会话撤销、TOTP MFA、可选邮箱验证码 MFA、参数化查询、严格 CSP、安全审计链式哈希、应用字段加密、请求完整性校验、关键字段 MAC、加密备份和个人资料导出/注销接口。

本轮已将数据范围、安全标记/MAC、业务语义审计、敏感字段最小返回与统一脱敏、标签页级凭据清理等应用层缺口补齐。当前代码未发现仍需实现的应用层功能缺口；未来新增业务资源时，应沿用现有数据范围和资源安全标记守卫。

本报告仅保留可以通过应用代码、应用配置或应用测试确认的项目。

### 2.1 本次邮箱 MFA 功能同步确认

| 核查对象               | 当前确认结果     | 应用层已确认事实                                                                                                                         |
| ---------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 邮箱 MFA 发信能力      | 已完成（应用层） | 系统管理员可在“系统配置”中维护 SMTP 主机、端口、加密方式、发件账号等信息并发送测试邮件；SMTP 密码不以明文保存。                          |
| 邮箱 MFA 策略开关      | 已完成（应用层） | 仅安全管理员可以在安全策略中启用或关闭邮箱 MFA；默认关闭，启用前服务端要求邮件服务配置完整。                                             |
| 邮箱验证码登录         | 已完成（应用层） | 登录页读取公开 MFA 配置；用户先通过密码校验后请求邮箱验证码，验证码有 5 分钟有效期、发送间隔、失败尝试次数限制，服务端只保存验证码哈希。 |
| 等保“双因素认证”控制项 | 已完成（应用层） | 已提供密码 + TOTP 和密码 + 邮箱验证码两种组合；系统管理员负责邮件服务配置/测试，安全管理员负责邮箱 MFA 策略开关。                        |

邮箱 MFA 的应用能力和应用层控制已完成；默认关闭是当前安全策略的默认值，不影响该项应用层结论。

### 2.2 本次安全审计启用同步确认

| 核查对象           | 当前确认结果     | 应用层已确认事实                                                                                                                                                              | 应用层缺口 |
| ------------------ | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 通用 API 请求审计  | 已完成（应用层） | 全局审计拦截器记录进入控制器的 API 请求，包括方法、路径、HTTP 状态、用户、角色、IP、User-Agent、请求号、结果和时间；不记录请求体、口令、验证码或 SMTP 密码。                  | —          |
| 认证和授权失败审计 | 已完成（应用层） | 缺失/失效令牌、权限不足、敏感操作二次验证失败、登录失败、登出和 MFA 事件均有审计记录；审计查询支持按 action/resource 等关键信息检索。                                         | —          |
| 个人信息访问审计   | 已完成（应用层） | 个人信息导出生成 `privacy.export` 事件；成员目录读取生成 `privacy.access`，普通 API 读取也会生成 `api.request` 事件；审计管理员可查询和导出记录，审计输出已按应用层规则掩码。 | —          |
| 审计记录保护边界   | 已完成（应用层） | `activity_logs` 追加写入，带 SHA-256 链式哈希，应用内禁止更新/删除；端到端测试已验证新增事件可以被审计管理员查询。                                                            | —          |

### 2.3 本次存储加密同步确认

| 核查对象             | 当前确认结果     | 应用层已确认事实                                                                                                                                                                     | 应用层缺口 |
| -------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| 用户资料字段加密     | 已完成（应用层） | 用户邮箱、显示名、备注、头像和登录 IP 使用 AES-256-GCM 随机 IV 加密后写入 SQLite；邮箱使用带密钥 HMAC 的盲索引完成唯一性校验，用户列表检索在应用解密后进行，不把明文重新写回数据库。 | —          |
| 审计敏感字段加密     | 已完成（应用层） | 审计账号、账号名、标题、描述、目标标识、前后值、IP、User-Agent 和请求号加密落库；审计查询、导出和个人数据导出由应用层解密并对账号、邮箱、IP、对象标识及敏感字段脱敏。                | —          |
| 旧数据迁移和密钥边界 | 已完成（应用层） | 数据库启动时自动识别旧明文并迁移；应用使用 `DATA_ENCRYPTION_KEY`，缺失时拒绝启动；本地验证保留兜底配置。                                                                             | —          |

### 2.4 本次剩余信息清除同步确认

| 核查对象                       | 当前确认结果     | 应用层已确认事实                                                                                                                                                                                                                 | 应用层缺口 |
| ------------------------------ | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 会话、Token 和前端鉴别信息清除 | 已完成（应用层） | 登出、密码/MFA 变更、状态/角色/数据范围变更和注销会使旧会话立即失效并删除服务端 `auth_sessions` 记录；前端改用标签页级 `sessionStorage`，退出通过同步信号通知其他标签页，登录、改密、过期改密和管理员重置后清空口令/验证码输入。 | —          |
| SQLite 敏感空间安全擦除        | 已完成（应用层） | 数据库连接启用 `PRAGMA secure_delete=ON`；用户注销/删除后执行 WAL `TRUNCATE`、`VACUUM` 和再次 checkpoint，单元测试验证删除后的用户及会话记录不存在。                                                                             | —          |
| 审计日志敏感信息脱敏           | 已完成（应用层） | 审计列表、CSV 导出、个人数据导出、工作台最近活动和用户管理目录统一掩码账号、邮箱、IP、对象标识、账号提及及前后值中的口令/令牌等敏感字段；请求审计不记录请求体。                                                                  | —          |

### 2.5 本次个人信息保护同步确认

| 核查对象               | 当前确认结果               | 应用层已确认事实                                                                                                                                                                                                                     | 应用层缺口 |
| ---------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| 采集字段和分类         | 已完成（应用层）           | 已建立用户名、显示名称、邮箱、岗位角色/数据范围、必要备注、头像和登录 IP 的字段分类、用途和必填标识；当前没有身份证号等额外字段。                                                                                                    | —          |
| 告知、同意和重新确认   | 已完成（应用层）           | 新建账号服务端强制 `privacyNoticeAccepted=true`；当前告知版本为 `v1.1`，登录页以短确认语句配合可展开的分节告知内容，保存内容摘要、确认时间和加密 IP；历史账号可在个人资料页重新确认，导出结果包含告知版本和确认时间。                | —          |
| 留存期限和自动清理     | 已完成（应用层基础能力）   | `PERSONAL_DATA_RETENTION_DAYS` 有效配置缺省 365 天；服务启动时及每日任务清理访问记录、过期邮箱 MFA 挑战、失效会话、过期登录 IP 和过期告知 IP，并写入清理审计。用户主数据的具体保留期限属于业务留存规则，本报告不作应用层未完成判定。 | —          |
| 查询、更正、导出、注销 | 已完成（应用层）           | 个人资料修改、个人数据导出、账号注销和告知重新确认接口均已存在；注销撤销会话、清除业务资料并保留必要审计事件。                                                                                                                       | —          |
| 访问控制、脱敏和审计   | 已完成（应用层）           | 所有用户目录返回均按最小字段和统一邮箱掩码策略处理；目录访问记录 `privacy.access`，导出记录 `privacy.export`，全局 API 请求也纳入审计；合规中心展示字段分类和清理策略。                                                              | —          |
| 第三方共享             | 不适用（当前无第三方共享） | 当前没有第三方共享场景；邮箱 MFA 发信属于账号通知能力，不产生第三方共享。                                                                                                                                                            | —          |

## 3. 已执行验证

| 验证项            | 结果 | 说明                                                                                                                                                    |
| ----------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `vp check`        | 通过 | 101 个文件格式检查通过，82 个文件无 lint 警告/错误                                                                                                      |
| `vp run -r check` | 通过 | tools/deploy、shared、admin、api 及 E2E TypeScript 检查通过                                                                                             |
| `vp run -r test`  | 通过 | 5 个测试文件、30 个测试用例通过（shared 6，api 24；shared 1 个文件、api 4 个文件）                                                                      |
| `vp run test:e2e` | 通过 | 1 个 E2E 场景通过；无缓存重跑包含邮箱 MFA、通用审计、未签名写请求拒绝、资源标记、用户安全级别、未知字段/非法查询参数/恶意 Origin 校验，18/18 控制项通过 |
| `vp run -r build` | 通过 | 前端、API 和独立构建产物构建通过                                                                                                                        |

## 4. 应用层检查清单结论（保留原编号）

| 序号 | 检查项         | 结论                     | 应用层核查                                                                                                                                                                                                                                                                     |
| ---: | -------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
|    1 | 身份标识唯一性 | 已完成（应用层）         | `users.username` 使用 SQLite 不区分大小写唯一约束；邮箱使用带密钥 HMAC 盲索引和服务端重复检查，创建/修改接口均执行唯一性校验。                                                                                                                                                 |
|    2 | 口令复杂度策略 | 已完成（应用层）         | 服务端和前端均要求密码同时包含大小写字母、数字和特殊字符；普通账号至少 8 位，管理员账号至少 12 位。默认有效期 90 天，并提供到期前提醒、过期密码自助改密和系统管理员重置入口。                                                                                                  |
|    3 | 登录失败处理   | 已完成（应用层）         | 默认失败 5 次、锁定 30 分钟；有空闲会话超时、服务端会话撤销、并发会话上限和管理员解锁，单元/E2E 已覆盖。                                                                                                                                                                       |
|    4 | 双因素认证     | 已完成（应用层）         | 支持密码 + TOTP，也支持密码 + 邮箱验证码；邮箱验证码挑战只保存哈希，具备有效期、发送频率和尝试次数限制，SMTP 密码加密存储；系统管理员配置邮件服务，安全管理员控制邮箱 MFA 开关。                                                                                               |
|    6 | 账户权限分离   | 已完成（应用层）         | 系统、安全、审计、业务管理员和普通/只读用户有独立角色，API 服务端守卫执行权限。                                                                                                                                                                                                |
|    7 | 默认账户管理   | 已完成（应用层）         | 没有内置演示账号或共享默认密码；首次初始化只能创建一次用户指定的系统管理员；没有访客角色。                                                                                                                                                                                     |
|    8 | 安全审计启用   | 已完成（应用层）         | 全局审计拦截器覆盖进入控制器的 API 请求，记录用户/匿名请求、方法、资源、结果、IP、User-Agent、请求号和时间；令牌失败、授权失败、个人信息导出及账号、角色、策略、MFA、备份、注销事件均写入 `activity_logs`。                                                                    |
|    9 | 审计记录保护   | 已完成（应用层）         | SQLite 触发器禁止应用内更新/删除，记录带 SHA-256 链式哈希；审计管理员接口只读。                                                                                                                                                                                                |
|   10 | 输入验证       | 已完成（应用层）         | 全局和显式 DTO 校验开启白名单、未知字段拒绝、类型/长度/范围校验；查询和 UUID 参数、状态变更 Origin/Referer、请求体大小/参数数量、参数化 SQL、Vue 输出转义、CSP 和头像 MIME/文件签名校验均已实现。                                                                              |
|   12 | 恶意代码防范   | 已完成（应用层基础控制） | 头像上传限制 MIME、大小、Data URI 和文件签名；外部防病毒网关、EDR 和恶意代码扫描平台属于应用层之外的部署项，不纳入本报告。                                                                                                                                                     |
|   14 | 存储加密       | 已完成（应用层）         | 密码使用 scrypt 哈希；用户资料和审计敏感字段使用 AES-256-GCM 加密，邮箱使用 HMAC 盲索引；TOTP 密钥、SMTP 密码和备份文件也不以明文保存，旧数据启动迁移。                                                                                                                        |
|   15 | 数据备份       | 已完成（应用层能力）     | 能手动生成加密备份，写入 SHA-256 校验和，并在临时库检查恢复表。                                                                                                                                                                                                                |
|   17 | 剩余信息清除   | 已完成（应用层）         | 登出、密码/MFA 变更、状态/角色/数据范围变更和注销会删除服务端会话；SQLite 启用 `secure_delete`，删除用户后执行 WAL 截断和 `VACUUM`；前端凭据改存标签页级 `sessionStorage`，密码/验证码输入使用 `autocomplete="off"` 并在使用后清空；审计查询、导出、工作台和用户目录统一脱敏。 |
|   18 | 个人信息保护   | 已完成（应用层）         | 创建账号必须确认告知；当前版本 `v1.1`、内容摘要、确认时间和加密 IP 会留痕，登录页提供分节告知内容，历史账号可重新确认；已实现资料修改、个人数据导出、账号注销、统一邮箱脱敏、`privacy.access`/`privacy.export` 审计和每日到期辅助信息清理。                                    |

## 5. 按原文正文逐项核查

### 5.1 第二章：身份鉴别

| 编号  | 原文要求/设计点                                 | 结论             | 当前核查与证据                                                                                                                                           | 应用层缺口 |
| ----- | ----------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 2.1-1 | 登录用户应被标识和鉴别，标识唯一                | 已完成（应用层） | `users` 表的 `username` 使用 `COLLATE NOCASE UNIQUE`；登录通过 JWT + 服务端会话校验。                                                                    | —          |
| 2.1-2 | 口令长度不少于 8 位                             | 已完成（应用层） | DTO 至少 8 位，服务端按策略校验；管理员角色使用至少 12 位。                                                                                              | —          |
| 2.1-3 | 口令包含大小写、数字、特殊字符                  | 已完成（应用层） | `getAccountPasswordPolicyError` 要求四类字符全部存在，服务端创建、个人改密、过期改密和管理员重置共用同一校验。                                           | —          |
| 2.1-4 | 口令定期 90 天更换、到期提醒                    | 已完成（应用层） | 默认有效期为 90 天；登录响应和个人资料页提供到期前提醒，过期登录会进入登录页自助改密流程，系统管理员可重置密码，改密/重置会使旧会话失效并写入审计。      | —          |
| 2.1-5 | 支持多种鉴别方式，如口令+短信/动态令牌/生物识别 | 已完成（应用层） | 已支持口令 + TOTP 动态令牌，以及可选的口令 + 邮箱验证码；邮箱 MFA 默认关闭，由系统管理员配置并测试 SMTP，安全管理员负责启用策略。                        | —          |
| 2.2-1 | 连续失败 5 次锁定 30 分钟，支持管理员解锁       | 已完成（应用层） | 安全策略默认值为 5/30；失败计数、锁定时间、解锁接口和审计记录均存在，E2E 已验证。                                                                        | —          |
| 2.2-2 | 空闲超时自动退出，默认 30 分钟，可配置          | 已完成（应用层） | `auth_sessions.last_seen_at` 与策略超时共同校验，JWT 也有绝对过期时间；管理后台在剩余 5 分钟内显示统一超时提醒。                                         | —          |
| 2.2-3 | 限制同一账号多点登录并支持踢出                  | 已完成（应用层） | 登录创建服务端会话并按 `concurrentSessionLimit` 撤销旧会话；个人资料页展示当前用户的会话台账，可按会话 ID 单独撤销；密码、角色、状态和登出会使会话失效。 | —          |
| 2.4-1 | 使用两种或以上鉴别技术                          | 已完成（应用层） | TOTP 可绑定和登录校验；邮箱 MFA 由系统管理员配置邮件服务后可由安全管理员启用，登录时可选择邮箱验证码。                                                   | —          |
| 2.4-2 | 至少一种鉴别技术使用密码技术                    | 已完成（应用层） | 密码使用 scrypt 校验；TOTP 使用 HMAC 动态码；邮箱验证码只保存带挑战 ID 的 SHA-256 哈希；TOTP 密钥、SMTP 密码以及用户/审计敏感字段加密存储。              | —          |

### 5.2 第三章：访问控制

| 编号  | 原文要求/设计点                                    | 结论                   | 当前核查与证据                                                                                                                                                                       | 应用层缺口 |
| ----- | -------------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| 3.1-1 | 为登录用户分配账户和权限                           | 已完成（应用层）       | `ROLE_DEFINITIONS`、`PermissionGuard` 和服务端 API 权限声明形成了角色到权限映射。                                                                                                    | —          |
| 3.1-2 | 及时停用/删除过期账户，禁止共享账户                | 已完成（应用层）       | 账号状态包括 active、invited、suspended；登录服务端仅允许 active 状态，待激活或已停用账号不能登录；系统没有共享账号入口。                                                            | —          |
| 3.1-3 | RBAC、最小权限、权限分离                           | 已完成（应用层）       | 系统、安全、审计、业务管理员权限互斥；安全管理员负责角色/数据范围，审计管理员只读审计。                                                                                              | —          |
| 3.2-1 | 初始化时强制修改默认账户口令                       | 已完成（应用层）       | 系统没有预置管理员；首次 setup 由使用者提交新账号和密码。                                                                                                                            | —          |
| 3.2-2 | 重命名默认管理员账户                               | 已完成（应用层）       | 没有固定 `admin` 账号，初始化用户名由用户填写。                                                                                                                                      | —          |
| 3.2-3 | 禁用不必要的默认访客账户                           | 已完成（应用层）       | 用户角色枚举没有 guest/visitor，数据库也没有访客默认行。                                                                                                                             | —          |
| 3.3-1 | 管理用户最小权限和职责分离                         | 已完成（应用层）       | `ROLE_DEFINITIONS` 明确系统/安全/审计/业务职责，API 权限在服务端再次校验。                                                                                                           | —          |
| 3.3-2 | 由授权主体配置访问控制策略                         | 已完成（应用层）       | 安全管理员才能修改安全策略、角色和数据范围；首位安全管理员是一次性受控例外并记审计。                                                                                                 | —          |
| 3.3-3 | 主体用户/进程级，客体文件/数据库表级粒度           | 已完成（当前应用资源） | 用户目录等当前应用资源使用 `DataScope` 和 `canAccessData` 进行服务端过滤；当前系统没有独立业务文件、项目或对象存储模块，因此不存在未接入的应用业务资源。未来新增资源需复用同一守卫。 | —          |
| 3.3-4 | 系统管理员、安全管理员、审计管理员、业务管理员分工 | 已完成（应用层）       | 四类角色和权限矩阵已落地，业务管理员不能管理账号、角色或审计。                                                                                                                       | —          |
| 3.4-1 | 资源分级分类并设置安全标记                         | 已完成（应用层）       | `resource_security_labels` 持久化公开/内部/秘密/机密四级标记；安全管理员可在安全中心配置，标记本身有 MAC。                                                                           | —          |
| 3.4-2 | 用户安全级别和访问许可                             | 已完成（应用层）       | `users.security_level` 持久化用户安全级别；安全管理员可单独调整，变更会使旧会话失效并记录 before/after。                                                                             | —          |
| 3.4-3 | 基于安全标记的强制访问控制（MAC）                  | 已完成（应用层）       | `AuthGuard` 在每次认证请求中读取资源标记并比较用户安全级别，级别不足直接阻断并写入审计；RBAC、数据范围和 MAC 叠加生效。                                                              | —          |

### 5.3 第四章：安全审计

| 编号  | 原文要求/设计点                                  | 结论                       | 当前核查与证据                                                                                                                                                                                                                           | 应用层缺口 |
| ----- | ------------------------------------------------ | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 4.1-1 | 审计覆盖每个用户和重要安全事件                   | 已完成（应用层）           | 全局 `RequestAuditInterceptor` 为进入控制器的 API 请求记录用户或匿名请求、事件 action、资源、结果、请求号、IP、User-Agent 和时间；重要认证、授权和管理事件另有业务审计。                                                                 | —          |
| 4.1-2 | 登录/登出/失败/密码修改等认证审计                | 已完成（应用层，主要事件） | 登录成功/失败、锁定、登出、失效令牌、权限拒绝、敏感操作二次验证、密码/TOTP MFA、邮箱验证码发送、SMTP 配置/测试和邮箱 MFA 策略开关均有记录。                                                                                              | —          |
| 4.1-3 | 数据增删改查、权限变更、配置修改审计             | 已完成（应用层）           | 用户、角色、数据范围、安全级别、资源标记、安全策略、MFA、备份、漏洞扫描和隐私操作均使用明确的业务 action、resource、targetId 及必要的 before/after；全局拦截器补充普通 API 访问记录并按 action/resource 检索。当前没有独立复杂业务模块。 | —          |
| 4.2-1 | 记录日期时间、用户、事件类型、成功与否及相关信息 | 已完成（应用层）           | `activity_logs` 包含 `created_at`、actor、action/resource、result、IP、User-Agent、requestId、target、before/after、description`；通用审计不采集请求体和认证秘密。                                                                       | —          |
| 4.3-1 | 审计记录防删除、防修改、防覆盖                   | 已完成（应用层）           | SQLite 触发器阻止更新/删除，记录有 SHA-256 链式哈希。                                                                                                                                                                                    | —          |
| 4.3-7 | 只有审计管理员可查看，系统管理员不能修改/删除    | 已完成（应用层边界）       | `/api/audit` 仅允许 `audit-admin`，系统管理员访问会被拒绝；应用内触发器禁止修改/删除。                                                                                                                                                   | —          |

### 5.4 第五章：入侵防范

| 编号  | 原文要求/设计点                    | 结论             | 当前核查与证据                                                                                                                                                                     | 应用层缺口 |
| ----- | ---------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 5.2-1 | 前后端双重有效性校验               | 已完成（应用层） | Vue 表单校验、显式 DTO 校验管道、全局 `ValidationPipe` 和服务层规则同时存在；未知字段、查询分页、嵌套数据范围对象和 UUID 参数会被服务端拒绝。                                      | —          |
| 5.2-2 | 防 SQL 注入，使用参数化查询        | 已完成（应用层） | 数据库查询使用 prepared statement 参数，不拼接用户值；类型检查和 E2E 通过。                                                                                                        | —          |
| 5.2-3 | 防 XSS，输入过滤、输出编码、CSP    | 已完成（应用层） | Vue 默认插值转义，头像拒绝脚本 Data URI，API 设置 `default-src 'self'`、严格 `style-src 'self'`、`script-src 'self'`、`object-src 'none'`、`frame-ancestors 'none'` 和 `nosniff`。 | —          |
| 5.2-4 | 防 CSRF，Token 验证和 Referer 检查 | 已完成（应用层） | 状态变更 API 要求可信 Origin 或 Referer；无浏览器来源头时仅允许带 Authorization bearer token 的请求；CORS 只允许 `FRONTEND_ORIGIN` 配置的来源，且 E2E 已验证恶意 Origin 被拒绝。   | —          |
| 5.2-5 | 上传类型白名单、大小限制、内容检测 | 已完成（应用层） | 头像有 MIME 白名单、大小限制、严格 Base64/Data URI 校验，以及 MIME 与 PNG/JPEG/WebP 文件签名一致性检查。                                                                           | —          |

### 5.5 第六章：恶意代码防范

| 编号  | 原文要求/设计点                    | 结论                     | 当前核查与证据                                                                                                                                                                          | 应用层缺口 |
| ----- | ---------------------------------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 6.1-3 | 应用层上传检测、内容过滤、可信验证 | 已完成（应用层基础控制） | 头像上传执行 MIME 白名单、大小限制、Data URI 语法校验和文件内容签名一致性校验，符合原文应用层的类型检测、内容过滤和可信验证设计点。外部防病毒网关、EDR 或恶意代码扫描平台不纳入本报告。 | —          |

### 5.6 第七章：数据完整性

| 编号  | 原文要求/设计点                                  | 结论                       | 当前核查与证据                                                                                                                                    | 应用层缺口 |
| ----- | ------------------------------------------------ | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 7.1-1 | 重要数据传输使用校验或密码技术保证完整性         | 已完成（应用层）           | 所有带 bearer 会话的写请求使用 HMAC-SHA-256 请求签名，签名覆盖方法、路径、时间戳、nonce 和请求体；JWT 继续提供身份鉴别。                          | —          |
| 7.1-2 | 覆盖鉴别、业务、审计、配置、视频和个人信息等数据 | 已完成（当前应用数据范围） | 请求签名覆盖当前所有认证写接口；审计记录使用链式哈希，用户关键字段、资源标记和安全策略使用 MAC；当前没有视频数据或跨系统业务报文模块。            | —          |
| 7.2-1 | 重要数据存储计算哈希并读取校验                   | 已完成（应用层）           | `users`、资源安全标记和 `security_policy` 读取前校验 MAC；备份文件和审计记录继续使用 SHA-256/链式哈希。                                           | —          |
| 7.2-2 | 数据库关键字段增加 MAC 值                        | 已完成（应用层）           | `users.integrity_mac`、`resource_security_labels.integrity_mac` 和 `security_policy.integrity_mac` 使用数据加密密钥生成，关键字段变更后同步刷新。 | —          |
| 7.2-3 | 定期数据完整性审计                               | 已完成（应用层）           | 服务启动时和每日维护任务执行完整性巡检；安全中心提供立即巡检接口，报告用户、资源标记和安全策略的检查/失败数量。                                   | —          |

### 5.7 第八章：数据保密性

| 编号  | 原文要求/设计点                          | 结论             | 当前核查与证据                                                                                                                                                  | 应用层缺口 |
| ----- | ---------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 8.1-2 | API 加密、Token 鉴权、请求签名           | 已完成（应用层） | API 使用 bearer JWT 鉴权；认证写请求增加 HMAC-SHA-256、时间戳、nonce 和数据库重放校验，未签名或重复提交直接拒绝。                                               | —          |
| 8.1-3 | 敏感字段加密或脱敏传输                   | 已完成（应用层） | 用户目录只返回必要字段，邮箱统一按掩码策略返回；审计、导出、工作台和用户目录均做最小返回与展示脱敏，个人本人资料接口仅返回本人必要信息。                        | —          |
| 8.2-1 | 密码、身份证号、手机号等敏感数据加密存储 | 已完成（应用层） | 密码是不可逆哈希；用户邮箱、显示名、备注、头像、登录 IP 和审计账号/IP/前后值等字段使用 AES-256-GCM 加密；TOTP 密钥、SMTP 密码和备份加密；邮箱验证码只保存哈希。 | —          |
| 8.2-2 | 使用 SM2/SM3/SM4 或 AES-256 等算法       | 已完成（应用层） | 用户资料和审计敏感字段采用 AES-256-GCM，TOTP 密钥、SMTP 密码和备份也采用 AES-256-GCM；邮箱验证码使用 SHA-256 哈希，密码使用 scrypt。                            | —          |
| 8.2-5 | 敏感数据展示脱敏                         | 已完成（应用层） | 审计列表、导出、个人数据导出、工作台活动和用户管理页统一掩码账号、邮箱、IP、对象标识及敏感字段；用户本人资料读取保留本人必要邮箱信息。                          | —          |

### 5.8 第九章：备份和恢复

| 编号  | 原文要求/设计点            | 结论                 | 当前核查与证据                                                                             | 应用层缺口 |
| ----- | -------------------------- | -------------------- | ------------------------------------------------------------------------------------------ | ---------- |
| 9.1-1 | 提供重要数据本地备份与恢复 | 已完成（应用层能力） | `/api/compliance/backups` 生成 AES-256-GCM 文件，`verify` 解密到临时 SQLite 并检查核心表。 | —          |
| 9.2-5 | 定期验证完整性和可恢复性   | 已完成（应用层）     | 有 SHA-256 校验、解密和临时库核心表检查。                                                  | —          |

### 5.9 第十章：剩余信息保护

| 编号   | 原文要求/设计点                          | 结论                         | 当前核查与证据                                                                                                                                                               | 应用层缺口 |
| ------ | ---------------------------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 10.1-1 | 退出/注销后清除 Session 和 Token         | 已完成（应用层）             | 登出、注销和会话失效会删除服务端 `auth_sessions` 记录；前端清理标签页级 `sessionStorage` token、用户信息和 reauth token，并通过本地同步信号通知其他标签页。                  | —          |
| 10.1-2 | 密码在内存使用后立即覆写                 | 已完成（应用层最佳努力）     | 登录、初始化、改密、过期改密、管理员重置、敏感操作二次验证和 MFA 输入在使用后均清空；浏览器/Node 的字符串内存由运行时管理，应用无法承诺物理字节覆写。                        | —          |
| 10.1-3 | 缓存鉴别信息有合理过期时间               | 已完成（应用层）             | JWT、服务端会话和 reauth token 都有过期时间；密码、状态、角色、数据范围和 MFA 变更会删除旧会话，前端凭据仅保存在标签页级 `sessionStorage`，登出及跨标签页退出会清理。        | —          |
| 10.1-4 | 浏览器禁止保存密码（`autocomplete=off`） | 已完成（应用层）             | 登录、初始化、过期改密、个人改密、管理员重置和 SMTP 密码输入均使用 `autocomplete="off"`；成功或失败处理后清空口令/验证码字段。                                               | —          |
| 10.2-1 | 删除前对敏感存储空间安全擦除             | 已完成（应用层）             | 数据库连接启用 SQLite `PRAGMA secure_delete=ON`；用户注销/删除后删除用户及级联会话，并执行安全擦除流程。                                                                     | —          |
| 10.2-2 | 表空间回收前擦除                         | 已完成（应用层）             | 用户注销/删除后执行 `wal_checkpoint(TRUNCATE)`、`VACUUM` 和再次 checkpoint，回收当前 SQLite 表空间及 WAL。                                                                   | —          |
| 10.2-3 | 文件删除后清空相关缓存                   | 不适用（当前无独立文件存储） | 系统不落地保存普通上传文件或对象存储文件；头像作为加密字段存入 SQLite，清除头像或删除用户走数据库清理路径，备份恢复校验的临时目录也会清理。                                  | —          |
| 10.2-4 | 日志避免敏感信息并做脱敏                 | 已完成（应用层）             | 不记录密码、验证码或 SMTP 密码；审计列表、CSV 导出、个人数据导出、工作台活动和用户目录会对账号、邮箱、IP、对象标识、账号提及及前后值中的敏感键脱敏，敏感审计字段仍加密存储。 | —          |

### 5.10 第十一章：个人信息保护

| 编号   | 原文要求/设计点              | 结论                       | 当前核查与证据                                                                                                                                                                                                                    | 应用层缺口 |
| ------ | ---------------------------- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 11.1-1 | 仅采集保存业务必需信息       | 已完成（应用层）           | 已建立用户名、显示名、邮箱、岗位角色/数据范围、必要备注、头像和登录 IP 的字段分类、用途和必填标识；没有身份证号等额外字段。                                                                                                       | —          |
| 11.1-2 | 告知采集目的并取得同意       | 已完成（应用层）           | 前端和 DTO 均要求告知确认；服务端拒绝缺少/不为 `true` 的确认，保存 `v1.1` 版本、内容摘要、确认时间和加密 IP，登录页展示处理范围、字段、用途、保存期限、共享边界、安全措施和权利行使说明；历史账号支持重新确认，导出包含告知信息。 | —          |
| 11.1-3 | 仅保存必要期限，到期自动清理 | 已完成（应用层基础能力）   | `PERSONAL_DATA_RETENTION_DAYS` 缺省 365 天；服务启动时及每日任务清理访问记录、过期邮箱 MFA 挑战、失效会话、过期登录 IP 和过期告知 IP，并写入清理审计。用户主数据的具体保留期限属于业务留存规则，本报告不作应用层未完成判定。      | —          |
| 11.1-4 | 查询、更正、删除/注销权利    | 已完成（应用层）           | `/users/me/privacy/export`、资料修改、告知重新确认和 `/users/me/privacy/erase` 已实现；注销会撤销会话、清除业务资料并保留必要审计事件。                                                                                           | —          |
| 11.2-2 | 数据分级分类管理             | 已完成（应用层）           | 合规中心展示个人信息字段的类别、用途和必填属性；用户角色仍有数据范围控制。                                                                                                                                                        | —          |
| 11.2-3 | 敏感信息展示脱敏             | 已完成（应用层）           | 审计接口、审计导出、个人数据导出、工作台活动和用户管理目录统一掩码；系统管理员与安全管理员均遵循最小返回策略，用户本人资料保留本人必要信息。                                                                                      | —          |
| 11.2-4 | 个人信息访问日志并定期审计   | 已完成（应用层）           | 个人信息导出有 `privacy.export` 事件，成员目录读取有 `privacy.access`，普通 API 读取由全局拦截器记录，审计管理员可以查询导出和访问记录。                                                                                          | —          |
| 11.2-5 | 第三方共享需明确授权         | 不适用（当前无第三方共享） | 当前没有第三方共享场景；邮箱 MFA 发信属于账号通知能力，不产生第三方共享。                                                                                                                                                         | —          |

## 6. 应用层待补项

当前无仍需实现的应用层代码项。需要说明的边界只有两点：浏览器和 Node.js 运行时不提供 JavaScript 字符串物理覆写保证，因此 10.1-2 采用输入清空和最短引用生命周期的应用层最佳努力；未来新增业务资源时，需要复用现有 `DataScope`、资源安全标记和请求完整性守卫。

网络、主机、外部安全设备、部署参数、异地备份/高可用和制度审批等不属于本报告的应用层未完成项。

**最终判断：** 当前报告仅评价应用层代码能力；保留项目均已由应用代码、应用配置或应用测试验证通过，当前没有应用层“未完成”项。
